<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Aurelian Air — Private Aviation</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:wght@300;400;500&family=Inter:wght@300;400&display=swap" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        :root {
            --black: #0a0a0a;
            --charcoal: #1a1a1a;
            --graphite: #2a2a2a;
            --gray: #4a4a4a;
            --soft-gray: #9a9a9a;
            --off-white: #f5f5f0;
            --gold: #d4af37;
            --gold-light: #e5c158;
        }

        body {
            font-family: 'Inter', sans-serif;
            background-color: var(--black);
            color: var(--off-white);
            line-height: 1.6;
            overflow-x: hidden;
        }

        h1, h2, h3 {
            font-family: 'Cormorant Garamond', serif;
            font-weight: 300;
        }

        .fade-in {
            opacity: 0;
            transform: translateY(20px);
            transition: opacity 1.2s ease, transform 1.2s ease;
        }

        .fade-in.visible {
            opacity: 1;
            transform: translateY(0);
        }

        /* Jet Animation */
        @keyframes jetFly {
            0% {
                transform: translateX(-100vw);
                opacity: 0;
            }
            10% {
                opacity: 0.3;
            }
            90% {
                opacity: 0.3;
            }
            100% {
                transform: translateX(100vw);
                opacity: 0;
            }
        }

        .jet-line {
            position: absolute;
            top: 45%;
            left: 0;
            width: 300px;
            height: 1px;
            background: linear-gradient(90deg, transparent, rgba(212, 175, 55, 0.3), transparent);
            animation: jetFly 3s ease-in-out;
            z-index: 1;
            pointer-events: none;
        }

        /* Hero Section */
        .hero {
            height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            position: relative;
            background: linear-gradient(rgba(10,10,10,0.5), rgba(10,10,10,0.7)), 
                        url('https://images.unsplash.com/photo-1583574928147-c5e90f8e2622?w=1920&q=85&fit=crop');
            background-size: cover;
            background-position: center;
            text-align: center;
            padding: 0 5%;
            overflow: hidden;
        }

        .hero::after {
            content: '';
            position: absolute;
            inset: 0;
            background: radial-gradient(ellipse at center, transparent 0%, rgba(10,10,10,0.4) 100%);
            pointer-events: none;
        }

        .hero-content {
            max-width: 900px;
            z-index: 2;
            animation: heroFade 1.8s ease-out 3s backwards;
        }

        @keyframes heroFade {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .brand-name {
            font-size: clamp(4rem, 10vw, 8rem);
            font-weight: 300;
            letter-spacing: 0.2em;
            margin-bottom: 2.5rem;
            color: var(--off-white);
            text-transform: uppercase;
        }

        .tagline {
            font-size: clamp(1.2rem, 2.5vw, 1.8rem);
            font-weight: 300;
            letter-spacing: 0.05em;
            color: var(--soft-gray);
            margin-bottom: 4rem;
            font-style: italic;
        }

        .cta-primary {
            display: inline-block;
            padding: 22px 60px;
            background-color: transparent;
            color: var(--off-white);
            border: 1px solid var(--gold);
            text-decoration: none;
            font-size: 0.95rem;
            letter-spacing: 0.2em;
            text-transform: uppercase;
            transition: all 0.5s ease;
            font-family: 'Inter', sans-serif;
            font-weight: 400;
        }

        .cta-primary:hover {
            background-color: var(--gold);
            color: var(--black);
            transform: translateY(-2px);
        }

        /* Section Styling */
        section {
            padding: 140px 8%;
            max-width: 1400px;
            margin: 0 auto;
        }

        .section-title {
            font-size: clamp(3rem, 6vw, 5rem);
            font-weight: 300;
            letter-spacing: 0.08em;
            margin-bottom: 5rem;
            color: var(--off-white);
        }

        /* How Aurelian Works */
        .how-aurelian-works {
            background-color: var(--charcoal);
        }

        .work-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
            gap: 6rem;
            margin-top: 5rem;
        }

        .work-item {
            text-align: left;
        }

        .work-number {
            font-size: 1rem;
            color: var(--gold);
            letter-spacing: 0.15em;
            margin-bottom: 1.5rem;
            text-transform: uppercase;
        }

        .work-title {
            font-size: 2rem;
            font-family: 'Cormorant Garamond', serif;
            font-weight: 400;
            letter-spacing: 0.05em;
            margin-bottom: 1.5rem;
            color: var(--off-white);
        }

        .work-description {
            font-size: 1.1rem;
            line-height: 1.9;
            color: var(--soft-gray);
            font-weight: 300;
        }

        /* Interior Showcase */
        .interior-showcase {
            background-color: var(--black);
        }

        .interior-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 2px;
            margin-top: 5rem;
            background: var(--black);
        }

        .interior-item {
            aspect-ratio: 3/2;
            background-size: cover;
            background-position: center;
            position: relative;
            overflow: hidden;
        }

        .interior-item::after {
            content: '';
            position: absolute;
            inset: 0;
            background: rgba(10,10,10,0.15);
            transition: background 0.6s ease;
        }

        .interior-item:hover::after {
            background: rgba(10,10,10,0);
        }

        .interior-item.large {
            grid-column: span 2;
            aspect-ratio: 21/9;
        }

        /* What Happens Next */
        .what-happens {
            background-color: var(--graphite);
            text-align: center;
        }

        .process-steps {
            max-width: 800px;
            margin: 0 auto;
            display: flex;
            flex-direction: column;
            gap: 4rem;
        }

        .process-step {
            position: relative;
            padding: 2rem 0;
        }

        .process-step::after {
            content: '';
            position: absolute;
            bottom: -2rem;
            left: 50%;
            transform: translateX(-50%);
            width: 1px;
            height: 60px;
            background: linear-gradient(180deg, var(--gold), transparent);
        }

        .process-step:last-child::after {
            display: none;
        }

        .process-label {
            font-size: 0.9rem;
            color: var(--gold);
            letter-spacing: 0.15em;
            text-transform: uppercase;
            margin-bottom: 1rem;
        }

        .process-text {
            font-size: 1.4rem;
            font-family: 'Cormorant Garamond', serif;
            color: var(--off-white);
            line-height: 1.6;
        }

        /* Trust & Positioning */
        .trust {
            background-color: var(--black);
        }

        .trust-content {
            max-width: 900px;
            margin: 0 auto;
            text-align: center;
        }

        .trust-statement {
            font-size: clamp(1.3rem, 2.5vw, 2rem);
            line-height: 1.8;
            color: var(--soft-gray);
            margin-bottom: 4rem;
            font-weight: 300;
        }

        .trust-pillars {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 4rem;
            margin-top: 5rem;
        }

        .trust-pillar {
            text-align: center;
        }

        .trust-icon {
            font-size: 3rem;
            color: var(--gold);
            margin-bottom: 1.5rem;
            opacity: 0.8;
        }

        .trust-pillar-title {
            font-size: 1.2rem;
            font-family: 'Cormorant Garamond', serif;
            color: var(--off-white);
            margin-bottom: 0.8rem;
        }

        .trust-pillar-text {
            font-size: 1rem;
            color: var(--soft-gray);
            line-height: 1.6;
        }

        /* Waitlist Section */
        .waitlist {
            background-color: var(--charcoal);
            padding: 160px 8%;
        }

        .waitlist-container {
            max-width: 700px;
            margin: 0 auto;
            text-align: center;
        }

        .waitlist .section-title {
            margin-bottom: 2rem;
        }

        .waitlist-intro {
            font-size: 1.2rem;
            color: var(--soft-gray);
            margin-bottom: 5rem;
            line-height: 1.8;
        }

        /* Google Form Styling Override */
        .form-embed {
            width: 100%;
            background: rgba(26, 26, 26, 0.6);
            border: 1px solid var(--gray);
            padding: 3rem;
            margin-bottom: 2rem;
        }

        .form-embed iframe {
            width: 100%;
            min-height: 800px;
            border: none;
            background: transparent;
        }

        /* Custom Form Alternative (if Google Form not used) */
        .custom-form {
            background: rgba(26, 26, 26, 0.6);
            border: 1px solid var(--gray);
            padding: 3rem;
        }

        .form-group {
            margin-bottom: 2.5rem;
            text-align: left;
        }

        .form-label {
            display: block;
            font-size: 0.85rem;
            color: var(--soft-gray);
            margin-bottom: 0.8rem;
            letter-spacing: 0.1em;
            text-transform: uppercase;
        }

        .form-input,
        .form-select,
        .form-textarea {
            width: 100%;
            padding: 18px 22px;
            background-color: rgba(42, 42, 42, 0.8);
            border: 1px solid var(--gray);
            color: var(--off-white);
            font-size: 1.05rem;
            font-family: 'Inter', sans-serif;
            transition: all 0.3s ease;
        }

        .form-input:focus,
        .form-select:focus,
        .form-textarea:focus {
            outline: none;
            border-color: var(--gold);
            background-color: rgba(42, 42, 42, 1);
        }

        .form-textarea {
            min-height: 120px;
            resize: vertical;
        }

        .form-submit {
            width: 100%;
            padding: 22px;
            background-color: transparent;
            color: var(--off-white);
            border: 1px solid var(--gold);
            font-size: 0.95rem;
            letter-spacing: 0.2em;
            text-transform: uppercase;
            cursor: pointer;
            transition: all 0.5s ease;
            font-weight: 400;
        }

        .form-submit:hover {
            background-color: var(--gold);
            color: var(--black);
        }

        .form-confirmation {
            display: none;
            text-align: center;
            padding: 4rem 2rem;
        }

        .form-confirmation.active {
            display: block;
        }

        .confirmation-message {
            font-size: 1.8rem;
            font-family: 'Cormorant Garamond', serif;
            color: var(--gold-light);
            margin-bottom: 1rem;
        }

        .confirmation-subtext {
            font-size: 1.1rem;
            color: var(--soft-gray);
        }

        /* Footer */
        footer {
            background-color: var(--black);
            padding: 100px 8% 50px;
            text-align: center;
        }

        .footer-brand {
            font-size: 2.5rem;
            font-family: 'Cormorant Garamond', serif;
            letter-spacing: 0.15em;
            margin-bottom: 1rem;
            text-transform: uppercase;
        }

        .footer-tagline {
            font-size: 1rem;
            color: var(--soft-gray);
            letter-spacing: 0.1em;
            margin-bottom: 4rem;
        }

        .footer-cta {
            margin-bottom: 5rem;
        }

        .footer-links {
            display: flex;
            justify-content: center;
            gap: 3rem;
            margin-bottom: 3rem;
        }

        .footer-links a {
            color: var(--soft-gray);
            text-decoration: none;
            font-size: 0.95rem;
            letter-spacing: 0.05em;
            transition: color 0.3s ease;
        }

        .footer-links a:hover {
            color: var(--gold-light);
        }

        .footer-bottom {
            border-top: 1px solid rgba(74, 74, 74, 0.2);
            padding-top: 2rem;
            font-size: 0.85rem;
            color: var(--soft-gray);
            opacity: 0.4;
        }

        @media (max-width: 1024px) {
            .trust-pillars {
                grid-template-columns: 1fr;
                gap: 3rem;
            }

            .interior-grid {
                grid-template-columns: 1fr;
            }

            .interior-item.large {
                grid-column: span 1;
                aspect-ratio: 3/2;
            }
        }

        @media (max-width: 768px) {
            section {
                padding: 100px 6%;
            }

            .work-grid {
                grid-template-columns: 1fr;
                gap: 4rem;
            }

            .footer-links {
                flex-direction: column;
                gap: 1.5rem;
            }
        }
    </style>
</head>
<body>

    <!-- Hero Section -->
    <section class="hero">
        <div class="jet-line"></div>
        <div class="hero-content">
            <h1 class="brand-name">Aurelian Air</h1>
            <p class="tagline">Guaranteed arrival. Private when possible.</p>
            <a href="#waitlist" class="cta-primary">Request Access</a>
        </div>
    </section>

    <!-- How Aurelian Works -->
    <section class="how-aurelian-works">
        <div class="fade-in">
            <h2 class="section-title">How Aurelian Works</h2>
            <div class="work-grid">
                <div class="work-item">
                    <div class="work-number">01</div>
                    <h3 class="work-title">Off-Market Access</h3>
                    <p class="work-description">We connect you to empty-leg and repositioning flights not available through standard channels.</p>
                </div>
                <div class="work-item">
                    <div class="work-number">02</div>
                    <h3 class="work-title">Curated Selection</h3>
                    <p class="work-description">No mass-market listings. Every opportunity is vetted, verified, and matched to your requirements.</p>
                </div>
                <div class="work-item">
                    <div class="work-number">03</div>
                    <h3 class="work-title">Direct Control</h3>
                    <p class="work-description">Private communication. Discreet coordination. No intermediaries, no public platforms.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- Interior Showcase -->
    <section class="interior-showcase">
        <div class="fade-in">
            <h2 class="section-title">Experience</h2>
            <div class="interior-grid">
                <div class="interior-item large" style="background-image: url('https://images.unsplash.com/photo-1540962351504-03099e0a754b?w=1920&q=85&fit=crop');"></div>
                <div class="interior-item" style="background-image: url('https://images.unsplash.com/photo-1556388158-158ea5ccacbd?w=1200&q=85&fit=crop');"></div>
                <div class="interior-item" style="background-image: url('https://images.unsplash.com/photo-1436491865332-7a61a109cc05?w=1200&q=85&fit=crop');"></div>
            </div>
        </div>
    </section>

    <!-- What Happens Next -->
    <section class="what-happens">
        <div class="fade-in">
            <h2 class="section-title">What Happens Next</h2>
            <div class="process-steps">
                <div class="process-step">
                    <div class="process-label">Step One</div>
                    <p class="process-text">We review your submission and travel profile.</p>
                </div>
                <div class="process-step">
                    <div class="process-label">Step Two</div>
                    <p class="process-text">We qualify requirements and verify compatibility.</p>
                </div>
                <div class="process-step">
                    <div class="process-label">Step Three</div>
                    <p class="process-text">We grant early access to select routes and availability.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- Trust & Positioning -->
    <section class="trust">
        <div class="fade-in">
            <h2 class="section-title">Our Approach</h2>
            <div class="trust-content">
                <p class="trust-statement">
                    No pricing listed. No instant booking. No mass-market access.<br>
                    Aurelian Air operates on discretion, curation, and control.
                </p>
                <div class="trust-pillars">
                    <div class="trust-pillar">
                        <div class="trust-icon">—</div>
                        <h3 class="trust-pillar-title">Discreet</h3>
                        <p class="trust-pillar-text">Private coordination without public exposure.</p>
                    </div>
                    <div class="trust-pillar">
                        <div class="trust-icon">—</div>
                        <h3 class="trust-pillar-title">Curated</h3>
                        <p class="trust-pillar-text">Selective access, not open marketplace.</p>
                    </div>
                    <div class="trust-pillar">
                        <div class="trust-icon">—</div>
                        <h3 class="trust-pillar-title">Controlled</h3>
                        <p class="trust-pillar-text">Certainty in outcome, not speculation.</p>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Waitlist Section -->
    <section class="waitlist" id="waitlist">
        <div class="fade-in">
            <div class="waitlist-container">
                <h2 class="section-title">Request Access</h2>
                <p class="waitlist-intro">Early access is limited. Submit your information to be considered.</p>
                
                <!-- Option 1: Embedded Google Form (Replace YOUR_FORM_ID with actual Google Form embed URL) -->
                <!-- 
                <div class="form-embed">
                    <iframe src="https://docs.google.com/forms/d/e/YOUR_FORM_ID/viewform?embedded=true" frameborder="0" marginheight="0" marginwidth="0">Loading…</iframe>
                </div>
                -->

                <!-- Option 2: Custom Styled Form (Active by default) -->
                <div class="custom-form" id="waitlist-form-container">
                    <form id="waitlist-form">
                        <div class="form-group">
                            <label class="form-label">Full Name *</label>
                            <input type="text" class="form-input" name="name" required>
                        </div>
                        <div class="form-group">
                            <label class="form-label">Email Address *</label>
                            <input type="email" class="form-input" name="email" required>
                        </div>
                        <div class="form-group">
                            <label class="form-label">Role *</label>
                            <select class="form-select" name="role" required>
                                <option value="">Select one</option>
                                <option value="charter-client">Charter Client</option>
                                <option value="operator">Operator</option>
                                <option value="broker">Broker</option>
                                <option value="investor">Investor</option>
                                <option value="curious">Curious</option>
                            </select>
                        </div>
                        <div class="form-group">
                            <label class="form-label">Message (Optional)</label>
                            <textarea class="form-textarea" name="message" placeholder="Share your interest or typical routes..."></textarea>
                        </div>
                        <button type="submit" class="form-submit">Submit Request</button>
                    </form>
                </div>

                <div class="form-confirmation" id="form-confirmation">
                    <p class="confirmation-message">You're on the list.</p>
                    <p class="confirmation-subtext">Early access coming soon.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- Footer -->
    <footer>
        <div class="footer-brand">Aurelian Air</div>
        <p class="footer-tagline">Private Aviation. Curated Access.</p>
        
        <div class="footer-cta">
            <a href="#waitlist" class="cta-primary">Request Access</a>
        </div>

        <div class="footer-links">
            <a href="#waitlist">Early Access</a>
            <a href="mailto:contact@aurelianair.com">Contact</a>
        </div>

        <div class="footer-bottom">
            <p>© 2024 Aurelian Air. All rights reserved.</p>
        </div>
    </footer>

    <script>
        // Fade-in on scroll
        const observerOptions = {
            threshold: 0.1,
            rootMargin: '0px 0px -80px 0px'
        };

        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.classList.add('visible');
                }
            });
        }, observerOptions);

        document.querySelectorAll('.fade-in').forEach(el => {
            observer.observe(el);
        });

        // Smooth scroll
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    target.scrollIntoView({
                        behavior: 'smooth',
                        block: 'start'
                    });
                }
            });
        });

        // Form submission with confirmation
        const form = document.getElementById('waitlist-form');
        const formContainer = document.getElementById('waitlist-form-container');
        const confirmation = document.getElementById('form-confirmation');

        form.addEventListener('submit', function(e) {
            e.preventDefault();
            
            // Get form data
            const formData = new FormData(form);
            const data = Object.fromEntries(formData);
            
            // Here you would typically send data to your backend or Google Sheets
            // For now, we'll just show the confirmation
            console.log('Form submitted:', data);
            
            // Show confirmation, hide form
            formContainer.style.display = 'none';
            confirmation.classList.add('active');
            
            // Optional: Send to Google Sheets via Apps Script or external service
            // fetch('YOUR_GOOGLE_APPS_SCRIPT_URL', {
            //     method: 'POST',
            //     body: JSON.stringify(data)
            // });
        });
    </script>
</body>
</html>